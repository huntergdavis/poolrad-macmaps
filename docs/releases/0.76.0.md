# 0.76.0 — One companion options page

F29 puts fog, footprints, party layout, large message text and autosave together
under **Info → Options**. Settings → Companion options… opens the same page,
revealing a hidden companion when needed. The map-corner switches move here,
giving the map header more room.

Changes apply immediately. Existing preferences are retained, including each
area's fog/footprint choices. The page stays above the guest and scrolls to fit.

The universal Android build and 638 Java tests passed. Eight companion
navigation checks and 26 map/party render checks passed. Live tests verified
global persistence, immediate area-style updates, the Settings shortcut with
a hidden companion, and bounded layout. No physical-tablet retest is claimed.

The reusable UI helper reports checkbox state, and the render-check helper now
includes generated resources for navigation checks.
